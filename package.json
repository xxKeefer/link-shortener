{
  "name": "link-shortener-monorepo",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@10.11.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.5.5"
  }
}
